# Copy to .env (git-ignored) and change the password.
# These values are for the local docker-compose database only.
POSTGRES_DB=lorebound
POSTGRES_USER=lorebound
POSTGRES_PASSWORD=change-me
POSTGRES_PORT=5432
